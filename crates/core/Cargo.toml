[package]
name = "cotton2k-core"
version = "0.1.0"
edition = "2021"
description = "Shared data structures and utilities for the Cotton2K model."
license = "AGPL-3.0-or-later"

[dependencies]
chrono = "0.4"
csv = "1.4"
serde = { version = "1.0", features = ["derive"] }
ndarray = "0.17"
